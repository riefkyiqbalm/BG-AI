{
  "name": "my-app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "http-server .",
    "build": "npx @tailwindcss/cli -i ./styles/style-main.css -o ./dist/output.css --minify",
    "deploy:s3": "aws s3 sync . s3://your-s3-bucket-name --delete --acl public-read"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "http-server": "^14.1.1",
    "tailwindcss": "4.x.x",
    "@tailwindcss/cli": "4.x.x",
    "postcss": "^8.x.x",
    "autoprefixer": "^10.x.x"
  },
  "sources": {
    "negated": false  
  }
}